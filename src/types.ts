export type Menu = {
  id: string;
  image: string;
  title: string;
  description: string;
  price: string;
};

export type Service = {
  icon: string;
  text: string;
};

export type Testimonial = {
  review: string;
  author: string;
};
